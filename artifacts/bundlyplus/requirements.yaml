packages:
  "framer-motion": "For fluid page transitions, 3D floating animations, and high-end glassmorphism interactions"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility function to efficiently merge Tailwind CSS classes"
images:
  - path: "artifacts/bundlyplus/public/images/hero-3d-shapes.png"
    description: "3D rendered abstract geometric shapes (spheres, toruses, soft ripples) in light pastel pink, peach, and lavender colors, floating against a completely transparent background, high-end glossy soft-touch material, studio lighting."
    aspect_ratio: "16:9"
notes:
  - "Assuming local JSON data files are present at src/data/ as described."
  - "Assuming src/utils/logoUtils.ts is present as described."
  - "App uses a custom light pastel glassmorphism theme."
